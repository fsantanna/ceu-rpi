output (char* text)=>void CONSOLE_WRITE;
input int KEY;

var char[2] str;
    str[0] = 0;
    str[1] = 0;

await 1s;
call CONSOLE_WRITE => "Test...\n";

var char c;
every c in KEY do
    str[0] = c;
    call CONSOLE_WRITE => str;
end
